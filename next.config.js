/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: 'cdn.sanity.io',
        port: '',
        pathname: '/**',
      },
    ],
  },
  // Disable Fast Refresh to prevent config reload issues
  reactStrictMode: true,
}

module.exports = nextConfig
